# Temporary: runs one snapshot suite without the log formatter, so the failure
# message xcbeautify swallows is visible. Delete once the failure is understood.
name: Diagnose Snapshots

permissions:
  contents: read

on:
  push:
    branches:
      - widget-tile-icon-scaling

env:
  DEVELOPER_DIR: /Applications/Xcode_27.0.app/Contents/Developer
  BUNDLE_PATH: vendor/bundle

concurrency:
  group: diagnose-snapshots-${{ github.ref }}
  cancel-in-progress: true

jobs:
  diagnose:
    runs-on: xcode-27
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1

      - uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9  # v6.1.0
        name: "Restore: DerivedData"
        with:
          path: ~/Library/Developer/Xcode/DerivedData
          key: >-
            ${{ format('{0}-deriveddata-{1}-{2}',
            runner.os,
            env.DEVELOPER_DIR,
            hashFiles('Package.resolved')) }}
          restore-keys: >-
            ${{ format('{0}-deriveddata-{1}-',
            runner.os,
            env.DEVELOPER_DIR) }}

      - name: Resolve Swift packages
        run: >-
          xcodebuild -resolvePackageDependencies
          -project HomeAssistant.xcodeproj
          -scheme Tests-Unit

      - name: Run the snapshot suite unformatted
        continue-on-error: true
        run: >-
          xcodebuild test
          -project HomeAssistant.xcodeproj
          -scheme Tests-Unit
          -destination 'platform=iOS Simulator,name=iPhone 17,OS=latest'
          -only-testing:Tests-App/WidgetsSnapshotTests
          COMPILER_INDEX_STORE_ENABLE=NO
          > /tmp/raw.log 2>&1

      - name: Show what actually failed
        if: ${{ always() }}
        run: |
          echo "=== anything that reads as a failure ==="
          grep -inE "fail|issue|couldn't|exceeded|does not match|no reference" /tmp/raw.log \
            | grep -viE "failureCount|0 failures" | head -60 || true
          echo "=== tail of the run ==="
          tail -60 /tmp/raw.log || true
          echo "=== snapshot files touched during the run ==="
          git status --porcelain -- Tests | head -40 || true
