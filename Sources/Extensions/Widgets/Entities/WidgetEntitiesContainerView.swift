import WidgetKit
import SwiftUI

struct WidgetEntitiesContainerView: View {
    var entry: WidgetEntitiesEntry

    init(entry: WidgetEntitiesEntry) {
        self.entry = entry
    }

    var body: some View {
        Text("hi")
    }
}
